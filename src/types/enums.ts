export enum Status {
  UPCOMING = 'UPCOMING',
  INCOMPLETE = 'INCOMPLETE',
  COMPLETE = 'COMPLETE',
  INVOICED = 'INVOICED',
  PAID = 'PAID',
  RETIRED = 'RETIRED',
}

export enum Category {
  LOCKS= 'LOCKS',
  SHELVING = 'SHELVING',
  SHOWERS = 'SHOWERS',
  MIRRORS = 'MIRRORS',
}

export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
}